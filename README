Usage
=====

Create localhost.yml file with screensaver var
```
---
screensaver: "lightlocker"
panel: "xfce"
de: "xfce"
xmonad_docks: new
```

```
mkdir ~/repo &&
cd ~/repo &&
git clone https://github.com/nesaro/dotfiles.git &&
cd dotfiles && ansible-playbook deploy.yml
```
